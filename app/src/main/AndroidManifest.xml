<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.company.kaami">


    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            ></activity>
        <activity
            android:name=".activity.TermsServiceActivity"
            android:parentActivityName=".activity.LoginAndSignupActivity"
            android:theme="@style/NoActionbar" />
        <activity
            android:name=".activity.SignupActivity"
            android:parentActivityName=".activity.LoginAndSignupActivity"
            android:theme="@style/NoActionbar" />
        <activity
            android:name=".activity.LoginActivity"
            android:parentActivityName=".activity.SignupActivity"
            android:theme="@style/NoActionbar" />
        <activity
            android:name=".activity.LoginAndSignupActivity"
            android:theme="@style/NoActionbar" />
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>